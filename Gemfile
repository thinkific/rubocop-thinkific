# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in rubocop-thinkific.gemspec
gemspec

group :development, :test do
  gem 'rspec', '~> 3.4'
end
